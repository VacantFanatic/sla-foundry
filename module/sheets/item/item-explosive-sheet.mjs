import { SlaItemSheet } from '../item-sheet.mjs';

/**
 * Explosive item sheet (Application V2).
 * @extends {SlaItemSheet}
 */
export class SlaExplosiveItemSheet extends SlaItemSheet {
    static useTwoTabs = true;
    static useCataloguePart = true;
}
