import { SlaItemSheet } from '../item-sheet.mjs';

/**
 * Toxicant item sheet (Application V2).
 * @extends {SlaItemSheet}
 */
export class SlaToxicantItemSheet extends SlaItemSheet {
    static useTwoTabs = false;
    static useCataloguePart = true;
}
